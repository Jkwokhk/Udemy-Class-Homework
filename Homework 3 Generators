import random
def gensquares(N):
    for num in range(N):
        yield num**2

def rand_num(low,high,n):
    for num in range(n):
        yield random.randint(low,high)

s = 'hello'
s_iter = iter(s)

